git init
git status
git add filename
git rm --cached filename
git commit -m "adding testing.py file"
git restore filename

# diffrence between untracked ,staging and tracked.


<This is used to set your name and email globally in Git, meaning for all repositories on your system.you don't need to set again>

git config --global user.name "Harshadeep Sonawane"              (Sets your name as the author of every commit)
git config --global user.email "harshadeepsonawane50@gmail.com"  (Sets your email for commits...Email should match GitHub verified email)
git config --global --list                                       (To verify your Git configuration)


<After creating repository on github ..then in git you have to add github repo url.. Links your local Git repo to a GitHub repo>

git remote add origin https://github.com/Harshadeep0034/git_hub_for_devpos.git
git remote -v                                                                     (Shows remote URLs linked to your repo)
git remote set-url origin https://put_generatedt_token_here@file_url

(Updates the remote URL...Replaces old URL with PAT (Personal Access Token) based URL

Used when..1.Password auth is disabled 2.You want non-interactive authentication (CI/CD))

git push origin master

# git clone and git fork thier diffrence.

this is new changes  ...this message is pull from github to loacal and for this command is

git pull origin master

# After every change in file in local, we again have to add, commit and push to git hub

# branching stratergies : head means HEAD points to your current position in the repository(latest commit to your repo)

every branch in git is maintain its own copy

git branch dev 
#or
git checkout -b dev

git checkout dev
# OR
git switch dev


git log or git log --oneline

merge branches : through github pull request branches 

git fetch and git pull diffrence


# git hooks - first we have to create pre-hook file which we can create through the vs code and cli vim 
-- in vs code we can create by adding in .git file (if file is not show in explorer then go to chat setting-search user.exclude in file
 we have to cancle or delete the file the it will show on explorer) then add pre-commit file in which we can write script
 
 -- in cli command Get-ChildItem -Hidden for hidden folder. where we can see . git and in .git there is hooks folder. in hook folder createdfile 
 pre-commit.hook throuth vim